---
layout: post
title: Git 与 GitHub 基本使用
categories: [Knowledge, github]
description: 介绍 Git 的 SSH 配置、远程仓库管理等基本使用方法。
keywords: Git, GitHub, SSH, 版本控制
mermaid: false
sequence: false
flow: false
mathjax: false
mindmap: false
mindmap2: false
---

## 1、git 仓库管理

### 1.1 ssh 配置

![ssh-keygen](/images/posts/github/ssh-keygen.png)

1. 生成 SSH 密钥对

   ```
   ssh-keygen -t ed25519 -C "your_email@example.com"
   # 推荐使用更安全的 ed25519 算法
   ```

2. 将公钥添加到 GitHub 账户

   ```
   cat ~/.ssh/id_ed25519.pub
   ```

3. 测试 SSH 连接并开始使用

   ```
   ssh -T git@github.com
   ```

### 1.2 仓库管理

1. 远程仓库建立
2. 本地仓库初始化 `git init`
3. 添加远程仓库 `git remote add origin git@github.com:Loren3Ben/仓库名.git`
4. 更换远程仓库 `git remote set-url origin git@github.com:Loren3Ben/仓库名.git`
5. 查看当前配置的仓库 `git remote -v`
